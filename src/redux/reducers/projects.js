const initial_state = [
  "music-maker",
  "personal-site",
  "kanye-western",
  "bottle",
  "apple-copy",
  "horizon",
  "kin-no-mizusashi"
];

const projects = (state = initial_state) => {
  return(state);
}
export default projects